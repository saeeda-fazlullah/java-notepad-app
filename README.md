# Java Notepad Desktop Application

A simple desktop notepad application built in Java using Swing.  
The application provides text editing features such as opening, editing, and saving text files.

## 🛠️ Technologies Used
- Java
- Java Swing (GUI)

## 📂 Files
- **Notepad.java** → Entry point; opens the UI
- **FontChooser.java** 

## 🚀 How to Run
1. Open the project in Java IDE (NetBeans).
2. Compile both `Notepad.java` and `FontChooser.java`files.
3. Run.

## 📌 Features
- Simple Notepad interface  
- Text editing (Users can save as many text files as they want with different fonts,
  colours and sizes. Some features like Find, Replace, Time/Date, Select All, save as, Help, Undo, and Redo)
- Load & save files  
- Java Swing UI
  
